# Legacy Snapshots

This folder contains older, ad-hoc snapshots and command outputs that were previously committed at the repo root.

They are kept for reference, but should not be treated as authoritative for the current deployed state.

