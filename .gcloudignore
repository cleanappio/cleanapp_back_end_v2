.gcloudignore
.git
.gitignore
#!include:.gitignore

# Cloud Build needs these public CA certificate files for the runtime image.
# Our .gitignore ignores "*.cer" broadly, so we explicitly include these.
!docker_backend/certificates/**
!docker_pipelines/certificates/**

# Build metadata files are generated locally before `gcloud builds submit` and are safe to upload.
# They allow services to embed provenance into `/version` at build time.
!**/buildinfo.vars
